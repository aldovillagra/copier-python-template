# Activar automáticamente el entorno .venv de uv si existe
if [ -d .venv ]; then
    echo "▶ Activando .venv"
    source .venv/bin/activate
fi

# DEBUGGER
export PYTHONBREAKPOINT=ipdb.set_trace
# Python Path
export PYTHON_PATH=src
layout python .venv/bin/python
